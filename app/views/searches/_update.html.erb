<h3>Get tickets for your next race</h3>
<p class="lead">
  Filtered results by location:
</p>

<hr class="my-4">

<div class="row">
  <% @search.events.each do |event| %>
    <%= render 'event', event: event %>
  <% end %>
</div>

<%= link_to "&larr; home".html_safe, :root %>
